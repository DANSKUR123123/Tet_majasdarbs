<h1>{{ weather|json_encode(constant('JSON_PRETTY_PRINT') b-or constant('JSON_HEX_QUOT')) }}</h1>

<div>
    <form method="POST" method="{{ (weather|json_encode(constant('JSON_PRETTY_PRINT') b-or constant('JSON_HEX_QUOT'))) }}">
        <button>
            Refresh
        </button>
    </form>
</div>